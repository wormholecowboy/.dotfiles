---
description: Resume from .mem/ — load long.md + most recent daily
---

User's additional context: $ARGUMENTS

Invoke the `mem` skill in full-resume mode. Pass `$ARGUMENTS` as the topic/date/`list` arg if provided.
